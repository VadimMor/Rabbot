server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: dummy-route
          uri: http://localhost:8080
          predicates:
            - Path=/dummy/**