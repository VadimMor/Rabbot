server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service-public
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**
            
        - id: workspace-service-secured
          uri: http://localhost:8081
          predicates:
            - Path=/api/workspace/**
          filters:
            - JwtAuthenticationFilter
            
        - id: content-service-secured
          uri: http://localhost:8082
          predicates:
            - Path=/api/content/**
          filters:
            - JwtAuthenticationFilter