server:
  port: 8082

spring:
  application:
    name: content-service
  datasource:
    url: jdbc:postgresql://localhost:5433/rabbot
    username: rabbot
    password: rabbot
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true